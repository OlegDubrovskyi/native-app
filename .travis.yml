sudo: false
language: node_js
node_js: "8"

before_install:

- rm -rf node_modules
- npm cache clean --force

install:
- npm i --ignore-script
- tns cloud setup
- npm run run-android
- npm run build-android